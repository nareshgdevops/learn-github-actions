name: Dev - Apply
on: [workflow_dispatch]
jobs:
  Build:
    runs-on: self-hosted
    steps:
      - run: echo OK
  DEV:
    runs-on: self-hosted
    needs: Build
    steps:
      - run: echo OK
  TEST:
    runs-on: self-hosted
    needs: TEST
    steps:
      - run: echo OK
  DEV_DEPLOY:
    runs-on: self-hosted
    needs: TEST
    steps:
      - run: echo OK
  QA_DEPLOY:
    runs-on: self-hosted
    needs: DEV_DEPLOY
    steps:
      - run: echo OK
  UAT_DEPLOY:
    runs-on: self-hosted
    needs: QA_DEPLOY
    steps:
      - run: echo OK
  PROD:
    runs-on: self-hosted
    needs: UAT_DEPLOY
    steps:
      - run: echo OK